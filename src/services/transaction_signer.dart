class TransactionSigner {
  String signTransaction(String message, String privateKey) {
    // Sign the transaction using the private key
    return 'signature'; // For simplicity, returning a static signature
  }
}
